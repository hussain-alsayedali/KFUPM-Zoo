<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
 

</head>
<body>
  <%- include('partials/header') -%>
  <div class="">
    <div class="mt-6">
  
        <div class="flex justify-around max-md:bg-slate-300">
            <div class="flex flex-col w-1/3 items-center">
                <p><strong>User Name</strong>: <%= user.userName %></p>
                <p><strong>Email</strong>: <%= user.email %></p>
                <a href="/logout" class="btn-main mt-4 w-20">Logout</a>
                <a class="btn-main mt-8" href="/feed?type=all&page=0&sort=createdAt_desc">Return to Feed</a>
            </div>
  
            <div class="w-1/3">
              <ul class="flex justify-between mt-5 flex-wrap flex-col">
                <% for(var i=0; i<posts.length; i++) {%>
                  <li class=" mt-5 w-full m-12 border border-gray-400 px-8 pt-4 pb-8 rounded bg-slate-50">
                    <div class="flex justify-between mb-8">
                      <span class=""><%= posts[i].title%></span>
                      <span class=""><%= posts[i].score%></span>
                    </div>
                    
                    <a class="text-9xl " href="/post/<%= posts[i]._id%>">
                      <img class="" src="<%= posts[i].image%>">
                    </a>
                  </li>
                <% } %>
                </ul>
  
            </div>  
      </div>
    </div>
  </div>
 
</body>
</html>

